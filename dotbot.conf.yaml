- defaults:
    link:
      relink: True
      create: True

# Link configuration files to expected places
- link:
    ~/.zshenv: 1-zshenv
    ~/.zprofile: 2-zprofile
    ~/.zshrc:
      path: 3-zshrc
      force: true
    ~/.zlogin: 4-zlogin
    ~/.zlogout: 5-zlogout
